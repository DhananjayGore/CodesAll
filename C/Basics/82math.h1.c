/* double sqrt(double);
float fsqrt(float)
long double lsqrt(ld);

double cbrt(double);
float fcbrt(float)
long double lcbrt(ld);
*/
#include<stdio.h>
#include<conio.h>
#include<math.h>
main()
{
printf("square root of 16 is %f\n",sqrt(16));
printf("cube root of 125 is %f\n",cbrt(125));
}